import { UninformedSearch } from "./UninformedSearch";

export class DFS extends UninformedSearch {
	// eslint-disable-next-line @typescript-eslint/no-useless-constructor
	constructor(map: number[][]) {
		super(map);
	}
}
